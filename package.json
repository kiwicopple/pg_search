{
  "name": "pg_search",
  "version": "1.0.0",
  "description": "A Postgres extension for text search.",
  "main": "index.js",
  "scripts": {
    "reset": "supabase db reset && supabase gen types typescript --local > supabase/database.types.ts",
    "test": "DOTENV_CONFIG_PATH=.env.test jest --setupFiles=dotenv/config --config=jest.config.js --silent=false"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@supabase/supabase-js": "^2.21.0",
    "@types/jest": "^29.5.1",
    "crypto": "^1.0.1",
    "dotenv": "^16.0.3",
    "jest": "^29.5.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.4"
  }
}
